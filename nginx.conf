server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Serve .well-known files directly without SPA fallback
  location /.well-known/ {
    try_files $uri =404;
  }

  location / {
    try_files $uri /index.html;
  }
}
